<h1>Posts#index</h1>

<ul>  
<% @posts.each do |post| %> 
  <li><%= post.title %> <%= link_to 'Delete', post, :confirm=>"Delete this post ?", method: :delete %>
											  <%= link_to 'Show', post_path(post.id) %></li>
<% end %>
</ul>

<p><%= link_to 'New post', new_post_path %></p>
